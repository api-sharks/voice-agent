Replace these placeholder assets with real Whisper files when you are ready:

- whisper.js
- whisper.wasm
- ggml-tiny.bin

The project is wired so it can run immediately with the included mock `whisper.js`.
